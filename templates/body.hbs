<body class="{{options.theme}}">
    <div class="styleguide">
        <h1 class="title">{{options.title}}</h1>
        <ol>
            {{#each props}}
                {{>rule options=../options}}
            {{/each}}
        </ol>
    </div>
</body>
